============
inline block
============

block -> nil

---

(module (block (inline_suite (scope_start) (atom))))

=====
block
=====

block ->
    nil

---

(module (block (suite (scope_start) (atom))))
